version: "3.7"

services:
  app:
    build: .
    ports:
      - 8080:8080
    environment:
      DATABASE_URL: mongodb://mongodb:27017/nuocgpt
      SECRET_KEY: guiyfgc837tgf3iw87-012389764
      OPENAI_API_KEY: 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
      AWS_SECRET_ACCESS_KEY: hIlTGM4U6cDzrm++3snM4RmFhxG1d+5CAxIle0Bo
      AWS_ACCESS_KEY: AKIAYZWP2RCQZIKP4AML
      AWS_S3_BUCKET: nuocgpt-vectorstore
      AWS_REGION: ap-southeast-1
    depends_on:
      - mongodb
    volumes:
      - .:/app
    networks:
      - node-network

  mongodb:
    image: mongo:4.0.8
    restart: unless-stopped
    ports:
     - 27017:27017
    volumes:
      - mongodbdata:/data/db
    networks:
      - node-network

volumes:
  mongodbdata:
    driver: local

networks:
  node-network:
    driver: bridge
